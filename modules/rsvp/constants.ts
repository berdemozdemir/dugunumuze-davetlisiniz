/** Bir RSVP gönderiminde yanınızda getirilebilecek ek misafir üst sınırı. */
export const RSVP_MAX_COMPANIONS = 20;

/** Son etkinlik saatinden önce son rezervasyon için minimum zaman aralığı. */
export const RSVP_FINAL_EVENT_BUFFER_MS = 2 * 60 * 60 * 1000;
